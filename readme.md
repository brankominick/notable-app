# Brian K - calendar api

To run, in one terminal, call `python app.py`

In another terminal, call `pytest`

I lost track of time trying to figure out query string parameters with flask, so the app is incomplete.